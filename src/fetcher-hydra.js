'use strict'

const pino = require('pino')
const alog = pino({name: 'maintserv/hydra'})

const debug = require('debug')
const dlog = debug('maintserv:fetcher-hydra')

module.exports = (config, DBM, parentId) => {
  return {

  }
}
